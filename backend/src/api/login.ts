import { ApiCode, ApiReq } from "../../../shared/types";
import { ReqType, ResType } from "../types";
import { db } from "../db";

export function login(req: ReqType, res: ResType, data: ApiReq[ApiCode.Login]) {

}